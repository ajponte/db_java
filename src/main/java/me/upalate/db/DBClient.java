package me.upalate.db;

/**
 * A generic Database client which holds information
 * germane to multiple database instances (e.g. credentials
 * and connection information).
 */
public interface DBClient {

}
